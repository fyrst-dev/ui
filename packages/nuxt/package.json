{
    "name": "@fyrst/ui-nuxt",
    "version": "1.0.0",
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/types.d.mts",
            "import": "./dist/module.mjs"
        }
    },
    "main": "./dist/module.mjs",
    "typesVersions": {
        "*": {
            ".": [
                "./dist/types.d.mts"
            ]
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "prepack": "nuxt-module-build build",
        "dev": "bun run dev:prepare && cd playground && bun run dev",
        "dev:build": "cd playground && bun run prepare && nuxi build",
        "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
        "release": "bun run lint && bun run test && bun run prepack && changelogen --release && npm publish && git push --follow-tags",
        "lint": "eslint .",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
    },
    "dependencies": {
        "@nuxt/kit": "^4.0.1"
    },
    "devDependencies": {
        "@fyrst/design-preset": "workspace:*"
    },
    "peerDependencies": {
        "nuxt": "^4.0.1",
        "vue": "^3.5.12",
        "@pandacss/dev": "^1.0.1",
        "reka-ui": "^2.10.1"
    }
}